# 真源：仓库根 `.env.example`。此处为仅在 `packages/admin` 内执行 `npm run dev:node` 时的最小示例。
#
# `DATABASE_DRIVER` 与 Proxy Node 使用同一套规则（`@octafuse/core`）；Node 下省略视为 postgres。
# Cloudflare `npm run preview` 用 `.dev.vars`（见 `.dev.vars.example`），不要用 `DATABASE_URL`。
#
# 推荐在仓库根维护 `.env`，并执行 `npm run dev:admin:node`。
# 若仅在本包执行 `npm run dev:node`，可复制变量到本文件并取消注释，或：`ln -s ../../.env .env`
#
# DATABASE_DRIVER=postgres
# DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/cinatoken
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=replace-me
#
# `npm run dev:node` 在脚本中固定监听 8789（与 `npm run preview` 一致）。纯 `npm run dev` 仍为默认 3000。
